# Finger exercise: What is the value of the following expression?
# isinstance('ab', str) == isinstance(str, str)

# False:
# isinstance('ab', str) is True because the first argument is a string
# and the type is str
# isinstance(str, str) is False because the first argument is not an
# object but a type